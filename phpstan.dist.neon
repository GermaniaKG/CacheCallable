# phpstan.dist.neon

# Default config file for use in repo.
# Create a custom phpstan.neon to override settings

parameters:
    level: 2
    paths:
        - src

    ignoreErrors:

        - message: '#Stash#'
          path: src/CacheCallable.php
